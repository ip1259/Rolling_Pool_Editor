[project]
name = "rolling-pool-editor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "customtkinter>=6.0.0",
    "pyinstaller>=6.21.0",
    "sqlalchemy>=2.0.51",
]
